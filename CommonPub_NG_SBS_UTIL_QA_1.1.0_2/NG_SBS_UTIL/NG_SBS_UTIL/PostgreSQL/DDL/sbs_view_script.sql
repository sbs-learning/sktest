CREATE TABLE sbs_util.SBS_VIEW_SCRIPT 
(
	VW_ID					INTEGER GENERATED BY DEFAULT AS IDENTITY,
	FQ_VW_NAME				VARCHAR(200) UNIQUE,
	VW_SCRIPT				TEXT,
	RCRD_CREATE_USER_ID		VARCHAR(30) NOT NULL,
	RCRD_CREATE_IP			VARCHAR(30) NOT NULL,
	RCRD_CREATE_TS			TIMESTAMPTZ NOT NULL,
	RCRD_UPDT_USER_ID		VARCHAR(30),
	RCRD_UPDT_IP			VARCHAR(30),
	RCRD_UPDT_TS			TIMESTAMPTZ
);